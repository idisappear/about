window.CHAT_CONFIG = {
  // Recommended: set your backend proxy endpoint here, e.g. "https://your-api.example.com/chat"
  apiUrl: "",
  // Keep true only if you want browser-direct API key mode for quick testing.
  allowBrowserKey: true,
};
