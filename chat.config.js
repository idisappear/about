window.CHAT_CONFIG = {
  // After Render deploy, set your real backend URL here.
  // Example: "https://about-chat-backend.onrender.com/chat"
  apiUrl: "",
  // Keep false in production so browser never handles your OpenAI key.
  allowBrowserKey: false,
};
